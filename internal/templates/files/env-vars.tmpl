apiVersion: v1
kind: ConfigMap
metadata:
  name: env-vars-{{.Name}}
  labels:
    app: devenv-{{.Name}}
data:
  USER: "{{.Name}}"
  UID: "{{.GetUserID}}"
  IS_ADMIN: "{{.IsAdmin}}"
  GIT_NAME: "{{.Git.Name}}"
  GIT_EMAIL: "{{.Git.Email}}"
