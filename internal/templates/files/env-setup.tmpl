apiVersion: v1
kind: ConfigMap
metadata:
  name: env-setup-{{.Name}}
  labels:
    app: devenv-{{.Name}}
data:
  # Templated user setup script - processed with config values
  setup.sh: |
{{getTemplatedScript "user-setup.sh" . | indent 4}}